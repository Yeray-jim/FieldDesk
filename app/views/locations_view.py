"""Locations screen (integrated in Phase 6)."""

from __future__ import annotations

import flet as ft

from app.views.placeholder import PlaceholderView


class LocationsView(PlaceholderView):
    """Placeholder for the locations module."""

    title = "Ubicaciones"
    icon = ft.Icons.LOCATION_ON_OUTLINED
    message = (
        "Las ubicaciones se administran dentro de la ficha de cada cliente."
    )
